<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.inspirecenter.amazechallenge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden|adjustPan">

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.BlocklyActivity"
            android:label="@string/Code_designer"
            android:parentActivityName="org.inspirecenter.amazechallenge.ui.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.inspirecenter.amazechallenge.ui.MainActivity" />
        </activity>

        <activity android:name=".ui.GameActivity" />

        <activity android:name=".ui.AddPlayerActivity" />

        <activity
            android:name=".ui.AboutActivity"
            android:label="@string/About"
            android:parentActivityName="org.inspirecenter.amazechallenge.ui.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.inspirecenter.amazechallenge.ui.MainActivity" />
        </activity>

    </application>

</manifest>